{
  "name": "talks",
  "private": true,
  "type": "module",
  "scripts": {
    "new": "bun create slidev",
    "build": "bun run --filter '*' build",
    "lint": "bun run --filter '*' lint",
    "format": "bun run --filter '*' format",
    "prepare": "npx bunpare"
  },
  "workspaces": [
    "./*"
  ]
}
